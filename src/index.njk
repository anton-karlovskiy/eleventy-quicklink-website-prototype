---
title: Quicklink
layout: layouts/default.njk
description: Faster subsequent page-loads by prefetching in-viewport links during idle time.
sectionTitle: "## Summary"
sectionDescription:
  items:
    - "### Faster subsequent page-loads by prefetching in-viewport links during idle time."
    - "### Once initialized, `quicklink` will automatically prefetch URLs for links that are in-viewport during idle time."
---
{{ sectionTitle | markdown | safe }}
{% for item in sectionDescription.items %}
  {{ item | markdown | safe }}
{% endfor %}

{% include "components/quickstart-usage.njk" %}

{% include "components/npm-usage.njk" %}

{% include "components/use-with.njk" %}
